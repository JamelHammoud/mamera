import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jamelhammoud.mamera',
  appName: 'Mamera',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
